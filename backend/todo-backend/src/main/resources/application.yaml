spring:
  application:
    name: Todo Backend

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/todo_db
    username: postgres
    password: login@db

  sql:
    init:
      mode: always

server:
  port: 8080
